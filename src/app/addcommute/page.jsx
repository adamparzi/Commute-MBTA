import Main from "@src/components/MainBox";

export const metadata = {
    title: "Add A Commute",
  };

export default function AddCommutePage(){
    return (
        <Main>
            Add a commute: Ask what line/bus, direction, and stop here.
        </Main>
    )
}